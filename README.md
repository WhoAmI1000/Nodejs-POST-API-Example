# Nodejs-POST-API-Example

## Running The Code
Be sure you have the following dependencies installed first:
* express
* body-parser
* path

To run the file, open a terminal, navigate to the directory, and enter `node index.js`.
